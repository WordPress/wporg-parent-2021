{
	"name": "wporg-parent-2021-project",
	"version": "1.0.0",
	"description": "The codebase and development environment for the WordPress.org parent theme.",
	"author": "WordPress.org",
	"license": "GPL-2.0-or-later",
	"private": true,
	"dependencies": {},
	"devDependencies": {
		"@wordpress/env": "^4.5.0"
	},
	"scripts": {
		"initial-setup": "yarn && composer install && TEXTDOMAIN=wporg composer exec update-configs",
		"update-configs": "composer update && TEXTDOMAIN=wporg composer exec update-configs",
		"wp-env": "wp-env"
	},
	"workspaces": [
		"source/wp-content/themes/wporg-parent-2021"
	]
}
